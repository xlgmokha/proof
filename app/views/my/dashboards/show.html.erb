<div class="container">
  <div class="columns">
    <div class="column is-full">
      <h1 class="title"><%= t('.title') %></h1>

      <table class="table">
        <thead>
          <th></th>
        </thead>
        <tbody>
        <% @metadatum.each do |metadata| %>
          <tr>
            <td><%= metadata.entity_id %></td>
          </tr>
        <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
